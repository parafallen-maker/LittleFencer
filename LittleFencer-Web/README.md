# LittleFencer Web ⚔️

AI驱动的青少年佩剑训练助手 - Web PWA 版本

## 🚀 快速开始

### 启动本地服务器

```bash
cd LittleFencer-Web
chmod +x serve.sh
./serve.sh
```

或使用 Python 直接启动：

```bash
python3 -m http.server 8080
```

然后在浏览器打开: http://localhost:8080

### 在手机上使用

1. 确保手机和电脑在同一 WiFi 网络
2. 查看终端输出的 Mobile URL（如 `http://192.168.1.100:8080`）
3. 在手机浏览器打开该 URL
4. 点击"添加到主屏幕"即可像 App 一样使用

## ✨ 功能特性

- 📷 **实时摄像头** - 使用手机前置摄像头捕捉训练画面
- 🦴 **骨骼检测** - MediaPipe Pose 实时检测 33 个关键点
- ⚔️ **动作识别** - 检测前进步、后退步、弓步等动作
- 🎯 **质量评估** - 评估动作质量（完美/良好/可接受/需改进）
- 🗣️ **语音反馈** - TTS 语音即时纠正姿势
- 🔊 **音效反馈** - 不同质量动作播放不同音效
- 📹 **视频录制** - 录制训练视频并保存到本地
- 🔥 **连击系统** - Combo 计数器激励持续完成标准动作
- 📱 **PWA 支持** - 可安装到主屏幕，离线使用

## 🎮 使用说明

1. **点击"开始"按钮** - 开启摄像头和姿态检测
2. **站好位置** - 确保全身在画面中，距离约 2-3 米
3. **摆出 En Garde** - 预备姿势，系统会识别并显示绿色骨骼
4. **执行弓步** - 手臂先伸展，然后踢腿
5. **查看反馈** - 系统会评估动作质量并给出语音/视觉反馈
6. **点击"录制"** - 录制训练视频
7. **点击"回放"** - 查看已录制的视频

## 📁 项目结构

```
LittleFencer-Web/
├── index.html              # 主页面
├── manifest.json           # PWA 配置
├── sw.js                   # Service Worker
├── serve.sh                # 开发服务器脚本
├── css/
│   └── style.css           # 样式表
├── js/
│   ├── app.js              # 主应用入口
│   ├── camera.js           # 摄像头管理
│   ├── pose.js             # MediaPipe Pose 封装
│   ├── engine.js           # 击剑状态机
│   ├── feedback.js         # 语音/音效反馈
│   ├── recorder.js         # 视频录制
│   ├── skeleton.js         # 骨骼渲染
│   ├── ui.js               # UI 管理
│   ├── utils.js            # 工具函数
│   └── detectors/
│       └── index.js        # 动作检测器
└── assets/
    └── icons/              # PWA 图标
```

## 🛠️ 技术栈

| 组件 | 技术 |
|------|------|
| **姿态检测** | MediaPipe Pose (WASM/WebGL) |
| **语音反馈** | Web Speech API (SpeechSynthesis) |
| **音效** | Web Audio API |
| **视频录制** | MediaRecorder API |
| **离线支持** | Service Worker + Cache API |
| **存储** | LocalStorage / IndexedDB |

## 📱 浏览器兼容性

| 浏览器 | 支持 |
|--------|------|
| Chrome (Desktop/Mobile) | ✅ 完全支持 |
| Safari (iOS 14.5+) | ✅ 支持 |
| Firefox | ✅ 支持 |
| Edge | ✅ 支持 |
| 微信内置浏览器 | ⚠️ 部分支持 |

> 注意：iOS Safari 需要 14.5+ 版本才支持 MediaRecorder

## 🔧 开发

### 本地 HTTPS（可选）

某些浏览器要求 HTTPS 才能访问摄像头。可以使用 mkcert 生成本地证书：

```bash
# 安装 mkcert
brew install mkcert
mkcert -install

# 生成证书
mkcert localhost 127.0.0.1 ::1

# 使用带 SSL 的服务器
npx http-server -S -C localhost+2.pem -K localhost+2-key.pem
```

### 调试

打开浏览器开发者工具查看日志：
- `[App]` - 应用级日志
- `[Camera]` - 摄像头日志
- `[Pose]` - 姿态检测日志
- `[Engine]` - 状态机日志
- `[Feedback]` - 反馈系统日志
- `[Recorder]` - 录制日志

## 📄 License

MIT License

## 🙏 致谢

- [MediaPipe](https://mediapipe.dev/) - Google 的跨平台 ML 解决方案
- LittleFencer Android/iOS 原生应用
